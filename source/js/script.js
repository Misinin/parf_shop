const navigation = document.querySelector('.nav');
const navigationButton = document.querySelector('.nav__button');

const translateNavigationX = () => {
  navigation.classList.toggle('nav--active');
}
